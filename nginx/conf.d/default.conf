# 反向代理配置：默认 HTTP 直接可用；上线域名 + HTTPS 后，请参考本文件末尾的
# HTTPS 配置说明（同时可参考 README.md「HTTPS 配置」章节的 Let's Encrypt 申请步骤）。
#
# 部署到自己的域名前，请把下面所有 "your-domain.com" 替换为实际域名。

upstream frontend_upstream {
    server frontend:3000;
}

upstream backend_upstream {
    server backend:4000;
}

server {
    listen 80;
    listen [::]:80;
    server_name your-domain.com www.your-domain.com;

    include /etc/nginx/snippets/security-headers.conf;

    # 供 certbot webroot 方式申请证书使用
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # 后端 API
    location /api/ {
        proxy_pass http://backend_upstream;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 上传文件静态资源：直接由 Nginx 提供，不经过后端进程
    location /uploads/ {
        alias /usr/share/nginx/uploads/;
        add_header Cache-Control "public, max-age=2592000, immutable";
        access_log off;
    }

    # 前端 Next.js 应用
    location / {
        proxy_pass http://frontend_upstream;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# ------------------------------------------------------------------
# HTTPS 配置（申请到证书后启用）：
#
# 1. 先用上面的 HTTP server block 跑起来，确保 /.well-known/acme-challenge/ 可访问
# 2. 用 certbot 以 webroot 方式申请证书（见 README.md「HTTPS 配置」章节的具体命令）
# 3. 取消下面整段注释，把 your-domain.com 替换成实际域名
# 4. 在上面的 HTTP server block 里加一条重定向，把非 acme-challenge 的请求跳转到 HTTPS：
#      location / { return 301 https://$host$request_uri; }
# 5. 重新加载 Nginx：docker compose exec nginx nginx -s reload
#
# server {
#     listen 443 ssl;
#     listen [::]:443 ssl;
#     http2 on;
#     server_name your-domain.com www.your-domain.com;
#
#     ssl_certificate     /etc/letsencrypt/live/your-domain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     include /etc/nginx/snippets/security-headers.conf;
#     add_header Strict-Transport-Security "max-age=63072000; includeSubDomains" always;
#
#     location /api/ {
#         proxy_pass http://backend_upstream;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     location /uploads/ {
#         alias /usr/share/nginx/uploads/;
#         add_header Cache-Control "public, max-age=2592000, immutable";
#         access_log off;
#     }
#
#     location / {
#         proxy_pass http://frontend_upstream;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }
# }
